import { Layers as LayersIcon } from "@mui/icons-material";
import { IconButton } from "@mui/material";

export const LayerListButton = () => {
  return (
    <IconButton>
      <LayersIcon fontSize="small" />
    </IconButton>
  );
};
